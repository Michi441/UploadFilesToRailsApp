<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<% @post.images.each do |img| %>

  <%= image_tag img, style: "width: 200px;"%>
  <%= link_to "x", remove_attachment_post_path(img), method: :delete%>

<% end %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
